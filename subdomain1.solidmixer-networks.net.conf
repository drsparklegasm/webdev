## Config file for domain 1

server {
    listen 80;
    server_name subdomain1.solidmixer-networks.net;
    root /var/www/subdomain1;

    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}